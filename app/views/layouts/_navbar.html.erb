  <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm ">
    <span class="my-0 mr-md-auto font-weight-bold" >
      <%=link_to "SportForecasts", home_path %></span>
    <span class="p-2 text-dark"><%= link_to "Добавление статьи", new_post_path %></span>
      <span class="p-2 text-dark"><%= link_to "О нас", about_path %></span>
    <%- if @current_user#user_signed_in?  %>
      <%=@current_user.name %>
      <span class="p-2 text-dark"><%= link_to "Выйти", auth_url, method: :delete%></span>
      <%-else %>
    <span class="p-2 text-dark"><%= link_to "Войти", auth_url %></span>
      <% end %>
  </div>
